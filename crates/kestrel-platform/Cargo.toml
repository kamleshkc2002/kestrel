[package]
name = "kestrel-platform"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Platform adapter boundaries for Kestrel"

[dependencies]
kestrel-core = { path = "../kestrel-core" }
libpulse-binding = "2.30.1"

[dev-dependencies]
tempfile = "3"
